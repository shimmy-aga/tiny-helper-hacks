# Quickstart

1. Place file you wan to share in folder
2. Edit `app.py` parameters so that it points to the correct path of file in folder
3. Run this command in terminal to locally run server `py -m uvicorn app:app --host 0.0.0.0 --port 4000`

